# Chat Drop

Place exported or copied conversation transcripts here.

Supported formats:

- `.md`
- `.txt`
- `.json`

The watcher moves processed files into `processed/` and failed files into `failed/`.
