Add to the main README:

## Development installation

```bash
python -m pip install --upgrade pip setuptools wheel
python -m pip install -e ".[test]"
```

The ATLAS Python package root is `backend/app`.
