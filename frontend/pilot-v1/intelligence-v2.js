(()=>{const nativeFetch=window.fetch.bind(window);window.fetch=(input,init={})=>{let url=typeof input==='string'?input:input?.url;if(url==='/api/pilot/ai/ask'){url='/api/pilot/intelligence/ask';if(typeof input==='string')input=url;else input=new Request(url,input);}return nativeFetch(input,init);};function token(){return localStorage.getItem('sris_access_token')}async function governedMissionAnalysis(button){const codeNode=document.querySelector('#detail-code');const answer=document.querySelector('#mission-answer');if(!codeNode||!answer)return;const raw=(codeNode.textContent||'').trim();const parts=raw.split('/').map(x=>x.trim()).filter(Boolean);const missionCode=parts[parts.length-1]||raw;const title=(document.querySelector('#detail-title')?.textContent||'').trim();const objective=(document.querySelector('#detail-objective')?.textContent||'').trim();const question=(document.querySelector('#detail-question')?.textContent||'').trim();const context=(document.querySelector('#detail-context')?.textContent||'').trim();const message=`Analisa esta missão persistente do SRIS. Distingue rigorosamente factos confirmados, declarações, inferências, hipóteses, lacunas de evidência, riscos, alternativas e próximos passos.\n\nMissão: ${missionCode} — ${title}\nObjetivo: ${objective}\nPergunta central: ${question}`;answer.classList.remove('empty');answer.textContent='A recuperar contexto, evidência e governação…';button.classList.add('loading');try{const res=await nativeFetch('/api/pilot/intelligence/ask',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${token()}`},body:JSON.stringify({message,context,mission_code:missionCode})});let data={};try{data=await res.json()}catch{}if(res.status===401){localStorage.removeItem('sris_access_token');location.href='/';return;}if(!res.ok){const detail=data?.detail;throw new Error(typeof detail==='string'?detail:(detail?.message||detail?.code||`Erro ${res.status}`));}answer.textContent=data.answer;const sourceCount=data?.context?.sources?.length||0;const cost=Number(data?.charged_eur||0).toFixed(4);const history=document.querySelector('#dialogue-history');if(history){const row=document.createElement('div');row.className='timeline-row';row.innerHTML=`<span class="timeline-dot"></span><div><strong>Análise governada concluída</strong><div class="note">${sourceCount} fonte(s) recuperada(s) · ${cost} € · ${new Date().toLocaleString('pt-PT')}</div></div>`;history.prepend(row);}}catch(err){answer.textContent=`Não foi possível concluir: ${err.message}`;answer.classList.add('empty');}finally{button.classList.remove('loading');}}document.addEventListener('click',event=>{const button=event.target.closest?.('#detail-analyze-btn');if(!button)return;event.preventDefault();event.stopImmediatePropagation();governedMissionAnalysis(button);},true);})();