const $=(s)=>document.querySelector(s),$$=(s)=>[...document.querySelectorAll(s)];const token=()=>localStorage.getItem('sris_access_token');let profile=null,missions=[],selectedMission=null;function logout(){['sris_access_token','sris_refresh_token','sris_org_id'].forEach(k=>localStorage.removeItem(k));location.href='/';}function errText(data,status){const d=data?.detail;if(typeof d==='string')return d;if(d?.message)return d.message;if(d?.code)return d.code;return data?.message||`Erro ${status}`;}async function api(path,options={}){const headers={...(options.headers||{})};if(!(options.body instanceof FormData))headers['Content-Type']='application/json';if(token())headers.Authorization=`Bearer ${token()}`;const res=await fetch(path,{...options,headers});let data={};try{data=await res.json()}catch{}if(res.status===401){logout();throw new Error('Sessão expirada.');}if(!res.ok)throw new Error(errText(data,res.status));return data;}const titles={overview:'Visão geral',mission:'Mission Intelligence',copilot:'Copiloto IA',billing:'Créditos e planos',account:'Conta'};function go(section){$$('.section').forEach(s=>s.classList.toggle('active',s.id===section));$$('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.section===section));$('#page-title').textContent=titles[section]||'SRIS';$('#sidebar').classList.remove('open');if(section==='mission'&&profile?.organization)loadMissions();window.scrollTo({top:0,behavior:'smooth'});}$$('.nav button').forEach(b=>b.addEventListener('click',()=>go(b.dataset.section)));$$('[data-go]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.go)));$('#menu-btn')?.addEventListener('click',()=>$('#sidebar').classList.toggle('open'));$('#logout-btn').addEventListener('click',logout);$('#logout-btn-2').addEventListener('click',logout);function money(v){return Number(v||0).toFixed(2)}function initials(name){return(name||'S').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()}function orgId(){return profile?.organization?.id||localStorage.getItem('sris_org_id')}function miBase(){return `/api/organizations/${encodeURIComponent(orgId())}/mission-intelligence`}function renderProfile(p){profile=p;const u=p.user||{},o=p.organization||{},ai=p.ai||{};if(o.id)localStorage.setItem('sris_org_id',o.id);$('#mini-name').textContent=u.full_name||u.email||'Utilizador';$('#mini-org').textContent=o.name||'Workspace';$('#avatar').textContent=initials(u.full_name);$('#welcome-title').textContent=`Olá${u.full_name?' '+u.full_name.split(' ')[0]:''}. O que precisa de decidir?`;['#balance-eur','#billing-balance','#copilot-balance'].forEach(s=>$(s).textContent=money(ai.credit_eur));$('#plan-name').textContent=(ai.plan||'pilot').replace(/^./,x=>x.toUpperCase());$('#model-name').textContent=ai.model||'—';$('#copilot-model').textContent=ai.model||'IA';$('#copilot-model-2').textContent=ai.model||'—';$('#requests-used').textContent=ai.requests_used||0;$('#requests-limit').textContent=ai.request_limit||0;$('#workspace-role').textContent=(o.role||'—').toUpperCase();$('#workspace-name').textContent=o.name||'—';$('#account-name').value=u.full_name||'';$('#account-email').value=u.email||'';$('#account-org').value=o.name||'';$('#account-role').value=o.role||'';const ready=ai.provider_configured&&ai.runtime_enabled;$('#ai-status').textContent=ready?'IA disponível':'IA por configurar';$('#provider-state').textContent=ready?'Operacional':'Configuração';renderLedger(ai.ledger||[]);}function renderLedger(rows){const el=$('#ledger');if(!rows.length){el.innerHTML='<div class="note">Sem movimentos.</div>';return;}el.innerHTML=rows.map(r=>{const amount=Number(r.amount_eur||0),label=r.kind==='trial'?'Crédito inicial':r.kind==='test_topup'?'Carregamento de teste':r.kind==='ai_usage'?'Utilização de IA':r.kind;return `<div class="ledger-row"><span><strong>${label}</strong><div class="note">${r.reference||''}</div></span><strong>${amount>0?'+':''}${amount.toFixed(4)} €</strong></div>`}).join('');}async function refresh(){renderProfile(await api('/api/pilot/profile'));}async function askAI(message,context,answerEl,button){answerEl.classList.remove('empty');answerEl.textContent='A analisar…';button?.classList.add('loading');try{const data=await api('/api/pilot/ai/ask',{method:'POST',body:JSON.stringify({message,context:context||null})});answerEl.textContent=data.answer;$('#last-charge').textContent=`${Number(data.charged_eur||0).toFixed(4)} €`;await refresh();return data;}catch(err){answerEl.textContent=`Não foi possível concluir: ${err.message}`;answerEl.classList.add('empty');throw err;}finally{button?.classList.remove('loading');}}$('#copilot-form').addEventListener('submit',async e=>{e.preventDefault();try{await askAI($('#copilot-message').value.trim(),$('#copilot-context').value.trim(),$('#copilot-answer'),e.submitter);}catch{}});function showMissionMode(mode){$('#mission-empty').classList.toggle('hidden',mode!=='empty');$('#mission-editor').classList.toggle('hidden',mode!=='editor');$('#mission-detail').classList.toggle('hidden',mode!=='detail');}function resetMissionForm(parent=null){$('#mission-form').reset();$('#mission-domain').value='cross_domain';$('#mission-kind').value='mission';$('#mission-priority').value='strategic';$('#mission-parent-id').value=parent?.id||'';$('#mission-code-label').textContent=parent?`SUB-MISSÃO DE ${parent.code}`:'NOVA MISSÃO';$('#mission-editor-title').textContent=parent?'Criar sub-missão':'Criar missão';$('#save-mission-btn').textContent=parent?'Criar sub-missão':'Criar missão';$('#cancel-mission-btn').classList.remove('hidden');$('#mission-message').className='alert hidden';showMissionMode('editor');}$('#new-mission-btn').addEventListener('click',()=>resetMissionForm());$('#empty-new-btn').addEventListener('click',()=>resetMissionForm());$('#cancel-mission-btn').addEventListener('click',()=>selectedMission?openMission(selectedMission.id):showMissionMode(missions.length?'detail':'empty'));$('#create-submission-btn').addEventListener('click',()=>selectedMission&&resetMissionForm(selectedMission));$('#detail-submission-btn').addEventListener('click',()=>selectedMission&&resetMissionForm(selectedMission));function missionRow(m){const indent=Math.min(Number(m.depth||0),5)*14;return `<button class="mission-item ${selectedMission?.id===m.id?'active':''}" data-mid="${m.id}" style="padding-left:${14+indent}px"><span><strong>${m.title}</strong><small>${m.code} · ${m.mission_kind==='program'?'Programa':'Missão'}${m.children_count?` · ${m.children_count} sub`:''}</small></span><span class="mission-state">${m.lifecycle_state||'active'}</span></button>`;}function renderMissionList(){const q=$('#mission-search').value.trim().toLowerCase();const rows=missions.filter(m=>!q||`${m.title} ${m.code}`.toLowerCase().includes(q));$('#mission-list').innerHTML=rows.length?rows.map(missionRow).join(''):'<div class="note">Nenhuma missão encontrada.</div>';$$('[data-mid]').forEach(b=>b.addEventListener('click',()=>openMission(b.dataset.mid)));}$('#mission-search').addEventListener('input',renderMissionList);async function loadMissions(){if(!orgId())return;try{missions=await api(`${miBase()}/missions`);renderMissionList();if(!selectedMission)showMissionMode(missions.length?'empty':'empty');}catch(err){$('#mission-list').innerHTML=`<div class="alert error">${err.message}</div>`;}}async function openMission(id){const m=await api(`${miBase()}/missions/${encodeURIComponent(id)}`);selectedMission=m;renderMissionList();$('#detail-code').textContent=m.path_codes?.join(' / ')||m.code;$('#detail-title').textContent=m.title;$('#detail-objective').textContent=m.objective||'—';$('#detail-question').textContent=m.central_question||'—';$('#detail-context').textContent=m.context||'—';$('#detail-meta').innerHTML=`<span>${m.mission_kind==='program'?'Programa':'Missão'}</span><span>${m.priority||'strategic'}</span><span>Rev. ${m.revision}</span><span>${m.lifecycle_state||'active'}</span>`;$('#mission-answer').textContent='Selecione “Analisar com IA” para produzir uma leitura analítica desta missão persistente.';$('#mission-answer').classList.add('empty');showMissionMode('detail');loadAttachments();loadHistory();}$('#mission-form').addEventListener('submit',async e=>{e.preventDefault();const box=$('#mission-message'),payload={title:$('#mission-title').value.trim(),objective:$('#mission-objective').value.trim(),context:$('#mission-context').value.trim(),central_question:$('#mission-question').value.trim(),parent_mission_id:$('#mission-parent-id').value||null,mission_kind:$('#mission-kind').value,domain:$('#mission-domain').value.trim()||'cross_domain',priority:$('#mission-priority').value,horizon:$('#mission-horizon').value.trim(),stakeholders:[]};e.submitter?.classList.add('loading');try{const m=await api(`${miBase()}/missions`,{method:'POST',body:JSON.stringify(payload)});box.textContent='Missão criada e persistida.';box.className='alert success';await loadMissions();await openMission(m.id);}catch(err){box.textContent=err.message;box.className='alert error';}finally{e.submitter?.classList.remove('loading');}});$('#detail-analyze-btn').addEventListener('click',async e=>{if(!selectedMission)return;const prompt=`Analisa esta missão persistente do SRIS. Distingue rigorosamente factos, hipóteses, lacunas de evidência, riscos, alternativas e próximos passos.\n\nMissão: ${selectedMission.code} — ${selectedMission.title}\nObjetivo: ${selectedMission.objective}\nPergunta central: ${selectedMission.central_question}`;try{await askAI(prompt,selectedMission.context,$('#mission-answer'),e.currentTarget);}catch{}});$$('[data-mission-tab]').forEach(b=>b.addEventListener('click',()=>{$$('[data-mission-tab]').forEach(x=>x.classList.toggle('active',x===b));$$('.mission-tab').forEach(x=>x.classList.toggle('active',x.id===`mission-tab-${b.dataset.missionTab}`));}));async function loadAttachments(){if(!selectedMission)return;try{const rows=await api(`${miBase()}/missions/${encodeURIComponent(selectedMission.code)}/attachments`);$('#attachment-list').innerHTML=rows.length?rows.map(a=>`<div class="attachment-row"><span><strong>${a.original_filename||a.filename||'Documento'}</strong><small>${a.extraction_status||'registado'}${a.byte_size?` · ${Math.ceil(a.byte_size/1024)} KB`:''}</small></span><span class="pill">${a.extension||'file'}</span></div>`).join(''):'<div class="note">Sem documentos carregados.</div>';}catch(err){$('#attachment-list').innerHTML=`<div class="alert error">${err.message}</div>`;}}$('#upload-file-btn').addEventListener('click',async e=>{if(!selectedMission)return;const file=$('#mission-file').files[0];if(!file)return alert('Selecione primeiro um documento.');const fd=new FormData();fd.append('file',file);e.currentTarget.classList.add('loading');try{await api(`${miBase()}/missions/${encodeURIComponent(selectedMission.code)}/attachments`,{method:'POST',body:fd});$('#mission-file').value='';await loadAttachments();}catch(err){alert(err.message);}finally{e.currentTarget.classList.remove('loading');}});async function loadHistory(){if(!selectedMission)return;try{const rows=await api(`${miBase()}/dialogues?mission_code=${encodeURIComponent(selectedMission.code)}`);$('#dialogue-history').innerHTML=rows.length?rows.map(d=>`<div class="timeline-row"><span class="timeline-dot"></span><div><strong>${d.status||'Sessão analítica'}</strong><div class="note">${d.created_at?new Date(d.created_at).toLocaleString('pt-PT'):''}</div></div></div>`).join(''):'<div class="note">Ainda não existem sessões interativas nesta missão.</div>';}catch(err){$('#dialogue-history').innerHTML=`<div class="note">Histórico indisponível: ${err.message}</div>`;}}$$('.topup').forEach(b=>b.addEventListener('click',async()=>{const box=$('#billing-message');box.className='alert hidden';try{const data=await api('/api/pilot/credits/test-topup',{method:'POST',body:JSON.stringify({amount_eur:Number(b.dataset.amount)})});box.textContent=`Carregamento de teste concluído. Novo saldo: ${money(data.credit_eur)} €.`;box.className='alert success';await refresh();}catch(err){box.textContent=err.message;box.className='alert error';}}));(async()=>{if(!token()){location.href='/';return;}try{await refresh();const plans=await api('/api/pilot/plans');if(plans?.plans?.length){const byCode=Object.fromEntries(plans.plans.map(x=>[x.code,x]));[['professional',1],['organization',2]].forEach(([code,i])=>{const p=byCode[code],el=$$('#plan-grid .price')[i];if(p&&el)el.innerHTML=`${Number(p.monthly_eur).toFixed(0)} €<span style="font-size:14px">/mês</span>`;});}}catch(err){console.error(err);}})();